describe('CreateCustomer', () => {
  it('Should be able to create a new customer', () => {
    expect(1).toBe(1);
  });

  it('Should be able to create two new customers with the same email', () => {
    expect(1).toBe(1);
  });
});
